# Courses Service

Node.js service that manages course data for Shikshak.

Setup

```bash
cd Backend/Courses
npm install
```

Run

```bash
npm start
```

Notes
- See `controllers/` and `routes/` for implementation details.
- Check `package.json` for scripts.
