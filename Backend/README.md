# Backend

This folder contains backend microservices used by Shikshak:
- `ApiGateway` — gateway and routes
- `Auth` — authentication service
- `Courses` — courses service

To run a service:

```bash
cd <service-folder>
npm install
npm start
```

Each service has its own README with details.
