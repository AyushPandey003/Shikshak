# ApiGateway

Node.js API Gateway for the Shikshak backend.

Setup

```bash
cd Backend/ApiGateway
npm install
```

Run

```bash
npm start
```

Notes
- Check `package.json` for available scripts and ports.
- Routes are in the `routes/` directory.
